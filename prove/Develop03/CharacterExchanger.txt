class CharacterExchanger //this class will be responsible for taking a string and replacing each character with 
                            //a different character, in this case, an underscore.  It will have a method for this
                            //and then send the string back to the caller
{


//member variables

//private string[] _word;
private string _stringWord;
private string _replaceChar;

//constructors

public CharacterExchanger(string word)
{
    _stringWord = word;

}

//getters and setters likely not needed for this class

//could include one to allow people the change the replacement character?


//methods


    public string ReplaceChars()
    {
        
        string word = _stringWord.Replace("a","_");
        return word;
    }









}